import { MedicalInstitution } from './medical-institution';

describe('MedicalInstitution', () => {
  it('should create an instance', () => {
    expect(new MedicalInstitution()).toBeTruthy();
  });
});
