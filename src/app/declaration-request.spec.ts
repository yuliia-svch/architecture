import { DeclarationRequest } from './declaration-request';

describe('DeclarationRequest', () => {
  it('should create an instance', () => {
    expect(new DeclarationRequest()).toBeTruthy();
  });
});
